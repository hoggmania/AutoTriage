package com.autotriage.common.evidence;

/** Calibrated evidence strength for policy decisions; finding severity is a separate concern. */
public enum EvidenceLevel {
    INSUFFICIENT,
    LIMITED,
    MODERATE,
    STRONG
}
